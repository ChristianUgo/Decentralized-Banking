import { ActionWorkspace } from "@/components/actions/ActionWorkspace";

export const metadata = { title: "Borrow" };

export default function BorrowPage() {
  return <ActionWorkspace kind="borrow" />;
}
